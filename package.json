{
  "scripts": {
    "watch:sass": "sass --watch src/scss:_site",
    "build:sass": "sass src/scss:_site",
    "watch:eleventy": "eleventy --serve",
    "build:eleventy": "eleventy",
    "start": "npm-run-all build:sass --parallel watch:*",
    "build": "npm-run-all build:*"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "sass": "^1.80.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
